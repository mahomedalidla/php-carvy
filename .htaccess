# Activar el motor de reescritura de URLs de Apache
RewriteEngine On

# No hacer nada para archivos y directorios existentes
# Esto asegura que peticiones a /api.php o /index.php funcionen directamente.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
